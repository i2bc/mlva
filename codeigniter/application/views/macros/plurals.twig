{% macro comments(value) %}
  {% if value > 1 or value == 0 %}
    {{value}} commentaires
  {% else %}
    1 commentaire
  {% endif %}
{% endmacro %}

{% macro votes(value) %}
  {% if value > 1 or value == 0 %}
    {{value}} votes
  {% else %}
    1 vote
  {% endif %}
{% endmacro %}
